case_study:
  target: dev
  outputs:
    dev:
      type: sqlite
      threads: 1
      database: 'data.sqlite'
      schema: 'main'
      schemas_and_paths:
        main: '/Users/tamarakuzmich/case-analytics-engineer-FELD-M-1/data.sqlite/orders.db'
      schema_directory: '/Users/tamarakuzmich/case-analytics-engineer-FELD-M-1/data.sqlite'
      #optional fields
      extensions:
        - "/path/to/sqlean/crypto.so"